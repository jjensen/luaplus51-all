SubDir LUAPLUS Src Modules coxpcall ;

IncludeModule copyfile ;

{

local SRCS =
		src/coxpcall.lua
;

LuaPlusAddSimpleModule coxpcall : $(SRCS) ;

local filename ;
for filename in $(SRCS)
{
	CopyFile coxpcall : $(LUA_LDIR)/$(filename:BS) : $(SUBDIR)/$(filename) ;
}

}
